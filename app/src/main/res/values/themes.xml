<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.SnapEats" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/splash_background</item>
    </style>
    <style name="Theme.SnapEats.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#0D1117</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <!-- Branding image is only supported on API 31+ and not handled by the compat library -->
        <item name="android:windowSplashScreenBrandingImage" tools:targetApi="31">@drawable/ic_snapeats_wordmark</item>
        <item name="postSplashScreenTheme">@style/Theme.SnapEats</item>
    </style>
</resources>